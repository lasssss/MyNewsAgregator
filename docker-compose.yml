services:
  dailynews:
    build: .
    container_name: dailynews
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - CHAT_ID=${CHAT_ID}
      - ADMIN_ID=${ADMIN_ID}
      - WEB_TOKEN=${WEB_TOKEN}
